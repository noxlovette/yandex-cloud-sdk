pre-commit:
  parallel: false
  commands:
    fmt:
      glob: "*.rs"
      run: cargo +nightly fmt
      stage_fixed: true
    clippy:
      glob: "*.rs"
      run: cargo clippy --all-features --all-targets -- -D warnings
